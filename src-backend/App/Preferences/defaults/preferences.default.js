/**
 * Preferences come in a heirarchy.  Base preference is a group.
 * optional subgroups are available to use as well.  Following that,
 * labels and values are available to use for key and value.
 */

module.exports = {
  version: '0.0.1',
  locations: {
    defaultProjectFolder: 'undefined',
  },
};
